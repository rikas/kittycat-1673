<h1>Missing cats! 🙀</h1>

<div class="alert alert-success"><%= notice %></div>

<p><%= link_to "I found a cat!", new_cat_path, class: "btn btn-secondary" %></p>

<div class="d-flex row gap-3">
  <% @cats.each do |cat| %>
    <div class="card" style="width: 18rem;">
      <%= image_tag cat.image_url, class: "card-img-top" %>
      <div class="card-body">
        <h5 class="card-title">
          <%= cat.color %>
          cat found in
          <%= cat.location %>
          (<%= cat.legs %>
          legs)
        </h5>
        <p class="card-text">
          Found on
          <%= cat.created_at.to_formatted_s(:short) %>
        </p>
        <%= link_to "This cat was retrieved! 😻",
        cat_path(cat),
        data: {
          turbo_method: :delete,
          turbo_confirm: "Are you sure you retrieved it?",
        },
        class: "btn btn-outline-primary" %>
      </div>
    </div>
  <% end %>
</div>
